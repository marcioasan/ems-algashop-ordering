#8.8. Isolando execução dos testes de integração - 6'50"
spring:
  application:
    name: ordering
  datasource:
    url: 'jdbc:h2:mem:ordering-test'
    username: sa
    password: '123'
  h2:
    console:
      enabled: 'true'
      settings.web-allow-others: 'true'
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: 'true'